Dotfiles installed.

zsh will require a restart, or reloading the config, e.g.

  source ~/.zshrc

tmux will require configuration to be reloaded, e.g.

  tmux source-file ~/.tmux.conf

To install tmux plugins, start tmux and press: Ctrl-a + I

To install Vim plugins, open vim and run: :PluginInstall

